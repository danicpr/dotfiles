# rclone.conf gestionado por chezmoi (privado, 0600).
# Se configura con `rclone config` (remote 'gdrive') o con ~/ansible/rclone-omp.sh.
# Tras modificarlo, actualizar el source con: chezmoi re-add ~/.config/rclone/rclone.conf
