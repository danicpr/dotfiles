{{- $name := (get . "name") | default (env "GIT_NAME") -}}
{{- $email := (get . "email") | default (env "GIT_EMAIL") -}}
{{ if and $name $email -}}
[user]
	name = {{ $name }}
	email = {{ $email }}
{{- end }}
[core]
[init]
	defaultBranch = main
[credential]
	helper = store
